---
layout: post
category: python
title: 'PyEx 02-03/08. Функции'
---

**Чтобы использовать локальные переменные в телефункции, можем объявить инструкцию global.**

```python
x = 2
y = 3

def printer():
    global x, y
    return x**2, y**3

print(printer())
>>> (4, 27)
```

**Функция как результат функции**

```python
# Функция для вычисления факториала
# и двойного факториала
def factor (mode=True ):
    def sf(n):
        s=1 # Начальное значение произведения
        i=n # Начальное значение индекса
        while i>1: # Условие
            s *=i # Умножение на индекс
            i-=1 # Уменьшение индекса на 1
        return s # Результат вложенной функции
        # Вложенная функция для вычисления
        # # двойного факториала числа
    def df (n):
        s=1 # Начальное значение произведения
        i=n # Начальное значение индекса
        while i>1: # Условие
            s *=i # Умножение на индекс
            i -=2 # Уменьшение индекса на 2
        return s
    if mode:
        return sf
    else:
        return df


print("5! = " , factor()(5))
print("5! = " , factor(True)(5))
print("5!! = " , factor(False)(5))
>>> 5 ! =  120
>>> 5 ! =  120
>>> 5 !! =  15
```
